import 'package:flutter/material.dart';

class PhotosModelScreen extends StatelessWidget {
  const PhotosModelScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PhotosModelScreen'),
      ),
      body: Center(
        child: Text('PhotosModelScreen'),
      ),
    );
  }
}
